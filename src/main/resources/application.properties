# Server settings
server.port=8080
server.servlet.session.timeout=-1



# Spring DevTools settings
spring.devtools.restart.quiet-period=1000
spring.devtools.restart.poll-interval=2000

# DataSource settings for PostgreSQL
spring.datasource.url=jdbc:postgresql://localhost:5432/postgres?currentSchema=public
spring.datasource.username=postgres
spring.datasource.password=***Esra***77
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# JPA settings
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.hibernate.ddl-auto=update

# Logging for SQL queries


logging.level.org.hibernate.SQL=debug
logging.level.org.hibernate.orm.jdbc.bind=trace
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE
logging.level.org.springframework.security=DEBUG

logging.level.com.e_commerce=INFO



# Tomcat-specific settings
# Optional: You can configure additional Tomcat settings if necessary
